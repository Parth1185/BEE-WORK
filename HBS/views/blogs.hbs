{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    {{!-- <ul>
        {{#each blogs as |blog|}}
        <h1>{{blog.title}}</h1>
        <img src={{blog.banner}} alt="">
        <p>{{blog.content}}</p>
        {{/each}}
    </ul> --}}
    {{!-- {{#if}}
    {{#each blogs}}
    <h1>{{title}}</h1>
    <img src={{banner}} alt="">
    <p>{{content}}</p>
    {{/each}}
    {{else}}
    <h1>No Blogs Found</h1>
    {{/if}}
    
</body>
</html>  --}}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
     <header>
        <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/blogpage">Blogs</a></li>
        </ul>
    </header>
    <h1>Blog Page</h1>
   
        {{!-- {{#each blogs as |blog|}}
         <h1>{{blog.title}}</h1>
        <img src={{blog.banner}}alt="">
        <p>{{blog.content}}</p>

        {{/each}} --}}
        {{#if blogs}}
        {{#each blogs}}
         <h1>{{title}}</h1>
        <img src={{banner}}alt="">
        <p>{{content}}</p>
        <a href="/blogs/{{id}}">view more</a>
        {{/each}}
        {{else}}
        <h1>No blogs found</h1>
        {{/if}}
   
</body>
</html>